import { Router } from "express";

// create router
const chatRouter = Router();

// define routes
chatRouter.post('/chat', )

chatRouter.get('/chat', )

chatRouter.get('/chat/:id', )

chatRouter.patch('/chat/:id', )

chatRouter.delete('/chat/:id', )

// export router 
export default chatRouter;